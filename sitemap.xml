---
layout: null
---
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
        xmlns:image="http://www.google.com/schemas/sitemap-image/1.1">
  {% assign latest_post = site.posts.first %}

  <url>
    <loc>{{ "/" | absolute_url | xml_escape }}</loc>
    <lastmod>{{ site.home_lastmod | default: "2024-01-15" }}</lastmod>
    <changefreq>monthly</changefreq>
    <priority>1.0</priority>
  </url>

  <url>
    <loc>{{ "/blog/" | absolute_url | xml_escape }}</loc>
    <lastmod>{{ latest_post.date | default: site.time | date_to_xmlschema }}</lastmod>
    <changefreq>daily</changefreq>
    <priority>0.9</priority>
  </url>

  {% assign static_pages = site.pages | sort: "url" %}
  {% for page in static_pages %}
    {% assign page_url = page.url | replace: "index.html", "" %}
    {% if page.sitemap != false
      and page_url != "/"
      and page_url != "/blog/"
      and page_url != "/sitemap.xml"
      and page_url != "/404.html"
      and page_url != "/blog.html" %}
  <url>
    <loc>{{ page_url | absolute_url | xml_escape }}</loc>
    <lastmod>{{ site.time | date_to_xmlschema }}</lastmod>
    <changefreq>{% if page_url == "/blog/" %}daily{% else %}monthly{% endif %}</changefreq>
    <priority>{% if page_url == "/blog/" %}0.9{% else %}0.6{% endif %}</priority>
  </url>
    {% endif %}
  {% endfor %}

  {% for post in site.posts %}
  <url>
    <loc>{{ post.url | absolute_url | xml_escape }}</loc>
    <lastmod>{{ post.last_modified_at | default: post.date | date_to_xmlschema }}</lastmod>
    <changefreq>monthly</changefreq>
    <priority>0.8</priority>
    {% if post.image %}
    <image:image>
      <image:loc>{{ post.image | absolute_url | xml_escape }}</image:loc>
      <image:title>{{ post.title | xml_escape }}</image:title>
    </image:image>
    {% endif %}
  </url>
  {% endfor %}
</urlset>
